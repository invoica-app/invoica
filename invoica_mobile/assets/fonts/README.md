# Fonts

Expected files (Satoshi — https://www.fontshare.com/fonts/satoshi):

- `Satoshi-Regular.otf` (weight 400)
- `Satoshi-Medium.otf` (weight 500)
- `Satoshi-Bold.otf` (weight 700)

All three are present and declared in the `fonts:` block of `pubspec.yaml`
(weights 400/500/700), matching `AppTypography`. Source:
`~/Downloads/Satoshi_Complete/Fonts/OTF/`.
